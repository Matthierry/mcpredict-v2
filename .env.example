# Browser configuration. The deployed app uses the same-origin Worker API.
VITE_API_BASE_URL=

# Worker secrets must be set with `wrangler secret put`; never commit values.
# FIVE_DOLLAR_API_KEY=
# SYNC_TOKEN=
